name: Simple GitHub Pages Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Build site
        run: |
          mkdir _site
          cp index.html _site/index.html
          cp main.js _site/ || true
          cp main.css _site/ || true
          cp local-storage-api.js _site/ || true
          cp -r css _site/ || true
          cp -r js _site/ || true
          cp -r images _site/ || true
          cp -r icons _site/ || true
          cp manifest.json _site/ || true
          # Add any other needed assets here

      - name: Upload to GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy
        uses: actions/deploy-pages@v4
